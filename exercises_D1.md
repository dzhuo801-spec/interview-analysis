# D1 练习（我自己写的）

> 规则：**先自己写，写不出来再问**。写完我来检查。
>
> 这里只写**新增**的练习代码。要让脚本跑起来，把下面的代码贴进 `src/day1.py` 对应位置，
> 或者单独存成 `src/exercise.py` 我都能看。

---

## 问题 1：预测（不用写代码，文字回答）

**如果过滤掉「的」「了」「是」这些停用词，你预测词频图的 Top 5 会变成什么？哪个词排第一？**

我的预测：

1. 这个
2. 现在
3. 老一辈
4. 很
5. 看重

**为什么这么猜**：过滤掉，就是让程序不再进行阅读，而进行下一项


### 📝 批改（2026-09-13）

**判定：方向 ✅ ／ 理由 ❌**

**选词很准。** 在**未过滤**的真实词频 Top15 里，你写的 5 个词的实际位置是：

| 你写的 | 真实排名 | 次数 |
|---|---|---|
| 这个 | 第 3 | 15 |
| 现在 | 第 4 | 14 |
| 老一辈 | 第 6 | 9 |
| 很 | 第 8 | 9 |
| 看重 | **第 9** | 9 |

5 个词里 4 个进了前 8 名 —— **排序直觉是对的**。

> 真实数据（`README.md` 记录的实测值）：
> `的:28  了:21  这个:15  现在:14  都:10  老一辈:9  还是:9  很:9  看重:9  仪式:9`
> 再往后：`回来:9  以前:8  我们:8  这边:8  婚俗:8`

**有一点可以确定**：原来的前两名只有 `的`(28) 和 `了`(21)，
所以去掉它们之后，`这个`(15) 和 `现在`(14) **必定升到第 1、第 2** —— 这部分你猜对了。

**但第 3~5 名先别急着认定。** 9 次那一档有 **6 个词并列**
（`老一辈` `还是` `很` `看重` `仪式` `回来`），你选中了其中 3 个。
它们谁排第 3、谁排第 5，取决于**并列时程序按什么顺序排** ——
这正是练习 A 做完之后你能亲眼看到的东西。

> ⚠️ 顺带一提：我上一版批改里把这 5 个词写成了"全部落在前 8 名"（实际 `看重` 是第 9），
> 还擅自断言了并列时"按字典序" —— 那句我没验证过，已删掉。
> **正好给你做个示范：没跑过的结论不要写。** 这条你自己也适用（铁律 2）。

**但理由错了。** 你写的是：

> 过滤掉，就是让程序不再进行阅读，而进行下一项

这句话把两件完全不同的事混成了一件：

| | 是什么 | 代码里长什么样 |
|---|---|---|
| **你理解的"过滤"** | 控制**流程** —— 让程序"跳过"某些步骤、去干下一项 | `continue` / `break` |
| **词频里的"过滤"** | 从**统计结果**里剔除噪声词 | 计数时先判断：是停用词就不计数 |

关键区别：**程序读进来的数据一行都没少，分出来的词也一个都没少。**
变的只是「最后那张统计表里有哪些词」—— 它**不改变程序走向**，只改变**输出内容**。

**自己验证这两问（文字回答就行）**：
1. 如果"过滤"真的是跳过阅读，那 `read_data` 读进来的行数会不会变少？跑一次看 `共读入 N 条记录` 变不变。
2. `count_by_speaker` 统计出的发言句数会不会变？

> ✏️ **待订正**：用**你自己的话**重写一遍"为什么这么猜"。写不出来说明还没想透，回去再想。

---

## 问题 2：为什么中文要用 jieba，不能用 split()？

（提示：跑 `py -c "print('我们这边的婚俗变化很大'.split())"` 看结果）

我的回答：print运行需要进行英文双引号或单引号，但用了split后中文的单引号居然没有消失，jieba是什么意思我就不知道了


### 📝 批改（2026-09-13）

**判定：❌ 三处误解**（这题是概念题，必须一次搞对，后面全靠它）

**误解 1 —— 以为 print 出来的引号是"内容的一部分，应该消失"**

`print(x)` 打印的是 `x` 的内容。但当 `x` 是**列表**时，Python 会用**列表的显示格式**印出来，
格式里每个元素两边的引号是**语法标记**，不是内容。

```python
print('我们这边的婚俗变化很大')     # → 我们这边的婚俗变化很大        没有引号
print(['我们这边的婚俗变化很大'])   # → ['我们这边的婚俗变化很大']    有引号
```

所以引号**不是"没消失"，而是"本来就不该消失"** —— 它在提示你："这是个列表，里面装着 1 个元素"。
看到引号，你应该**意识到结果是列表**，而不是觉得哪里出错了。

**误解 2 —— 不知道 `split()` 按什么切**

`split()` 不写参数 = **按空格切**。中文句子中间**没有空格**，所以整句话被当成 1 段 → 切成 1 个元素的列表。

**误解 3 —— 不知道 jieba 是什么**

`jieba` 是一个**第三方库**（不是 Python 自带的）。它靠词典 + 算法判断中文词边界，
所以能把没空格的句子切开。因为是第三方的，装好之前 `import jieba` 会报 `ModuleNotFoundError`。

**顺带一提**：你 `src/day1.py` 第 64 行早就在用它了 —— `words += jieba.lcut(row["text"])`。
所以你不是"没用过"，是"用过但不知道它是什么"。

**自己跑这两条（必须真跑，看懂 ≠ 会）**：

```bat
py -c "print('我们这边的婚俗变化很大'.split())"
py -c "import jieba; print(jieba.lcut('我们这边的婚俗变化很大'))"
```

第一条会打出**只含 1 个元素的列表**；第二条会打出切好的 **5 个词**。

**我跑出来的结果（2026-09-13 实测，对照用）**：
```
split()      → ['我们这边的婚俗变化很大']                     ← 1 个元素（整句）
jieba.lcut() → ['我们', '这边', '的', '婚俗', '变化很大']      ← 5 个词
```

> ⚠️ 注意最后一个是 **`变化很大`** 一个词，不是 `变化` + `很大` 两个。
> 我一开始也猜成了 6 个词，实跑才发现错了 —— **所以下面这句话你要记住**：
> **jieba 用默认词典切，不保证切得对。**
>
> 这不是废话：你 `README.md` 的「局限与改进」里已经写了这一条
> （"没有针对访谈场景做自定义词典，专业词汇可能被切错"）。
> 现在你亲眼看到了它长什么样 —— 这就是那条"局限"的**真实证据**。

> ✏️ **待订正**：重写问题 2 的答案，要求出现这三个词：**列表**、**空格**、**第三方库**。

---

## 练习 A：停用词过滤（对应 TODO 1）

**要求**：在 `day1.py` 里加一个停用词列表，词频统计时把这些词过滤掉。

我的思路（先用自己的话说一遍）：


> 🎯 **起步提示（2026-09-13 加，故意不给答案）**
>
> **完成标准 —— 三条都满足才算做完：**
> - [ ] 词频 Top15 里不再出现 `的`、`了`、`都`（当前数据里只有这三个挤进了前 15）
> - [ ] `共读入 50 条记录` —— **仍然是 50**
> - [ ] `受访者B: 16 句` 等发言统计 —— **一字不变**
>
> ⚠️ 第 2、3 条不是走过场，它们**正是问题 1 那个误解的判据**：
> 如果你以为"过滤 = 让程序跳过阅读"，就很容易把过滤写进 `read_data()` ——
> 那第一行就会从 50 变成更少。
> **正确做法下，读入行数和发言统计必须纹丝不动，变的只有词频表。**
> 做完回来对这三条，你自己就能验证概念有没有搞对。
>
> **思考提纲（先想清楚再动手）：**
> 1. 停用词表用什么数据结构存？列表，还是集合 `set`？想一下"判断某个词在不在里面"哪种更快。
> 2. 过滤该放在 `count_words()` **里面**，还是在它**外面**再筛一遍？两种写法的后果有什么不同？
> 3. `count_words()` 现在是 `words += jieba.lcut(row["text"])` 之后再 `Counter(words)`。
>    要在计数阶段跳过停用词，有几种改法？**哪种改动最小**（铁律 3：要最简单能跑的版本）。
> 4. 停用词表写死在代码里，还是单独放个文件？
>    （提示：你以后要处理真实的 30+ 份访谈，这张表会改很多次）
>
> **卡住了按你自己的规则问我**（别问"答案是什么"）：
> ```
> 我自己写的是：【贴你的代码】
> 报错是：      【贴完整报错】
> 我想实现：    【说清目标】
> 我哪里想错了？
> ```

我的代码：

```python
# 写在这里
```

---

## 练习 B：每个人平均每句话多少字

**要求**：输出形如 `访谈者: 平均 12.3 字`

我的思路：


我的代码：

```python
# 写在这里
```

---

## 练习 C（挑战）：把统计结果存成 CSV（对应 TODO 3）

**要求**：写一个 `save_csv()` 函数，把词频结果存成 `output/word_freq.csv`。

> ⚠️ 这个需要用到你还没学的**文件写入**语法。**先自己想**，想不出来就问我
> "csv.writer 怎么用"，我给你讲原理，你自己写。

我的代码：

```python
# 写在这里
```

---

## 我卡住的地方（诚实写，卡住不丢人，装懂才亏）

- 
- 
